# Minesweeper2D 作業規約

## Windowsアプリ・ゲームの共通配布規則

- この規則は個人開発のWindowsアプリ・ゲームの標準であり、今後の新規プロジェクトにも適用する。Web・スマートフォンの配布方法とフォルダー構成は別途標準化する。OreComicは今回の対象外。
- 開発用ZIPは原則として作成しない。Releaseを製品のバージョンごとのフォルダー（標準形 `bin/Release-<Version>/`）に出力し、その中のEXEを起動・検証する。完了報告のEXEリンクもその場所を指す。
- EXEと必要なDLL・データ等を同じ出力先に保持する。異なるバージョンの出力先を分離し、ビルド出力はGit管理対象外とする。
- 公開準備時だけ、選定・検証したReleaseから配布用ZIPを作成・検証し、Distributionへ配置する。通常のビルドでZIPを作成したり、公開済みZIPを更新したりしない。
- 既存ZIPは削除しない。開発用の過去ZIPはローカル保管を継続し、一括でGit追加しない。
- 公開済みの同じ版数への上書きを禁止。公開済み製品の版数を初版へ戻さない。
- 公開履歴・SHA256・最新バイナリー案内を更新してコミットし、公開指示に基づきpushする。配布物と対象ソースの対応を確認する。
- 詳細はRELEASE-POLICY.mdを参照。旧方式のスクリプトは次にビルド・開発するときに標準に合わせて整備してから使う。規約の記録だけで対応済みとは扱わない。
- 規約・配布手順だけの変更で既存バイナリの版数を変更しない。

## Androidの配布・フォルダー規約（2026-09-22）

- Windowsと共通のVERSION.txtを正とし、互換機能追加はマイナー更新、修正はパッチ更新。HISTORY.mdに日時と概要を記録し、検証後ローカルコミットを作成する。
- APKは `bin/Android/Release-<Version>/Minesweeper-<Version>.apk` に出力し、SHA256・マニフェスト・署名の検証結果を併置する。Windows出力と分離しGit対象外とする。
- AndroidはARM64/IL2CPP Release。Development Build、Script Debugging、Profiler、シンボル生成を無効化し、実際のAPKを検証する。Google Play向けAABは不要。
- 専用Release鍵を使用する。鍵とDPAPI暗号化パスワードは `.local/android-signing/` に保管しGitへ追加しない。秘密情報をソースやログに記録しない。同一アプリの更新には同一鍵を使う。
- 完成済みの同版APKを上書きしない。失敗した検証用ビルドはログとともにBuildsの作業領域へ退避して再試行できる。公開済み成果物は変更しない。
- 実機へのインストールとプレイ確認はAPK生成と別工程。未確認を確認済みと報告しない。公開・pushは別途指示の範囲のみ。
## Androidアプリ／ゲームの成果物・GitHub運用標準（2026-09-22追記）

この節を今後の個人開発のAndroid向け標準とする。既存のWindowsアプリ／WindowsゲームのReleaseビルド、GitHub運用、公開用ZIPとDistribution配置等の規約は変更せず、そのまま維持する。

### 正式成果物と実機確認

- Android向けの正式成果物はReleaseビルドとする。Development Build、Script Debugging、デバッグシンボル等を含まない、Release向けに最適化された成果物を作成する。
- 成果物はAPKとする。ただし案件ごとに別途指示がある場合はその指示に従う。
- Release APKはローカルの既存成果物管理規約に従い、製品とバージョンを識別できる形で保存する。具体的な出力先を各プロジェクトの規約に記録する。
- 実機検証では、このRelease APKそのものを対象端末へインストールして確認する。デバッグビルドでの動作確認だけをもってRelease確認完了とはしない。

### GitHubと配布の扱い

- AndroidのRelease APKはGitHub Releases等へ配置しない。Gitリポジトリの配布フォルダー等にもAPKを追加して配布しない。
- 「自己責任でインストールしてください」という形での野良APK配布も行わない。
- GitHubはソースコード、Unityプロジェクト、変更履歴等を公開・管理する場所として使用する。Android対応プロジェクトについてもGitHub管理自体は通常どおり行う。
- 一般ユーザー向けAPK配布、Google Play公開、公開用署名、ストア対応等は通常のリリース作業には含めない。必要になった場合のみ別途ユーザー指示に基づいて実施する。
- ローカル実機インストール用のAPK署名と、一般配布に向けた公開用署名・ストア対応は区別する。
- ローカルGit管理とGitHub管理の開始・push・公開を区別する既存規約は継続する。この標準の記録自体を、新規リポジトリ作成・公開範囲変更・pushの指示とは扱わない。

### Minesweeper2Dの今回の範囲

- Android対応のゴールは「Android向けRelease APKを生成し、そのRelease APKをGalaxy S26 Ultraへインストールして、実機で正常にプレイできることを確認する」までとする。一般ユーザーへのAPK配布は含めない。
- APKのローカル保存先は `W:/dev/Minesweeper2D/bin/Android/Release-<Version>/Minesweeper-<Version>.apk`。APKと実機検証結果はローカル保管し、GitHubではAPKを配布せず、ソースコード／Unityプロジェクト／変更履歴を参照できる状態にする。
- 既存のローカル実機インストール用署名鍵の管理は維持する。これは一般配布向けの公開用署名・ストア対応を開始する意味ではない。

### 未確定プラットフォーム

- WebアプリおよびAndroid以外のスマートフォン向け成果物は、成果物と配布方法が確定した時点で別途規約を追加する。現時点では推測して作成しない。
- 既存の「メンテナンスまたは新規開発時に標準化する」という記述も、成果物と配布方法の確定後に行う趣旨とする。Androidについては本節で標準化済みとする。
