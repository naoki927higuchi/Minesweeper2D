# プラットフォーム別の成果物・配布規約（2026-09-22）

## 適用範囲

- バージョン表記・HISTORY.md・バージョン更新コミットの共通規約は引き続き適用する。
- 配布方法とフォルダー構成はプラットフォームごとに定める。Windowsの規約をWeb・スマートフォンへ流用しない。
- 以下は個人開発のWindowsアプリおよびWindows向けゲームの標準とし、今後の新規プロジェクトにも適用する。既存の適用対象はAF-SDR（AF-SignalGeneratorを含む）、AudioConverter、Breakout3D、Minesweeper2D、NekoDungeon、ShareXImageEditorContext。
- OreComicはWebアプリであり、ブログ立ち上げ前に作成した既存プロジェクトのため今回の対象外。OreComicのメンテナンス時、または新規Webアプリ開発時にWeb向け規約を標準化する。
- スマートフォンアプリも、該当プロジェクトのメンテナンスまたは新規開発時に、そのプラットフォームの配布・フォルダー規約を標準化する。

## Windows：ローカル開発・起動

- 開発用ZIPは今後、原則として作成しない。通常のビルドや動作確認にZIP生成を組み込まない。
- AF-SDRの方式を標準とし、リリースビルドの出力先を製品バージョンごとに分ける。標準形は製品プロジェクト直下の `bin/Release-<Version>/`。例：`bin/Release-1.2.3/`。
- そのフォルダーへEXEと実行に必要なDLL・データ等を直接出力し、その場所のEXEを起動・検証する。EXEだけを別の共通フォルダーへコピーして起動しない。
- 完了報告の起動リンクも、対象バージョンの出力フォルダー内のEXEを指す。版数は製品のバージョン設定元と一致させる。
- 異なる製品・バージョンの出力を混在させず、旧バージョンの出力を新バージョンのビルドで上書きしない。出力と中間生成物はGit管理対象外とする。
- エンジン等に固有の出力構造が必要な場合も、バージョン別の出力先とそこからの起動を守り、具体的なパスをそのプロジェクトの規約へ記録する。
- MSIXやコンテキストメニューの登録確認など、インストールが必要な検証は別途行う。通常のEXE起動だけでそれらの検証を済ませたことにしない。

## Windows：公開用ZIP

- リモートへpushする公開準備の時点で、対応するソースと検証済みのリリースビルドを選び、配布用ZIPを作成・検証する。通常の開発ビルドごとには作成しない。
- 公開対象のZIP、SHA256、版数・変更概要をソリューション直下の `Distribution/` に配置してGit管理する。既存の `distribution/` はそのまま利用し、新規では `Distribution/` を用いる。
- 最新バイナリーの案内は `Distribution/README.md` に置き、配布ZIP・SHA256へのリンクを公開時の同じコミットで更新する。
- 公開済みZIPを同じ版数で上書きしない。既存の公開版数を1.0.0へ戻さない。通常のビルドで公開ZIPを更新しない。
- 既に存在するZIPは、開発用・公開用とも削除しない。既存のローカル開発ZIPはローカル保管を継続し、過去分を一括でGit追加しない。
- 公開用の梱包作業場所はGit管理対象外とする。既存スクリプトがZIP入力を必要とする場合は、公開準備で作ったZIPを渡す。開発用ZIPの常時作成を前提にしない。
- 公開準備・コミット・pushは分けて実行する。この規約の記録だけで新規リポジトリを自動的にPublicへ変更したり、未指定の成果物をpushしたりしない。

## 既存構成の扱い

- この記録は標準規約の確定であり、既存成果物の一括移動・削除・再ビルドは行わない。
- 既存プロジェクトのビルド設定やスクリプトが旧方式の場合、次にビルド・開発を行う際に、この規約に合わせて設定を整備してから実行する。規約更新だけで実装が対応済みとは扱わない。

## Minesweeper2Dの公開準備

- 既存ZIPの保管場所：`Builds/Releases/`。過去の成果物として保持し、削除しない。通常の開発用ZIP出力先とはしない。
- このプロジェクトの公開先：`Distribution/`。ZIP、SHA256、配布情報、最新版のREADMEを管理する。
- 現在のPrepare-Release.ps1はZIPを入力に取る。公開準備時に梱包・検証したZIPを指定して、`Prepare-Release.ps1 -Version <公開版数> -ZipPath <公開準備ZIP>` を実行する。スクリプトは自動でpushしない。
- Prepare-Release.ps1のコピー・チェックサム照合は、製品の動作・署名・パッケージ検証の代わりではない。ソースと対応する製品の検証を先に完了する。
- HISTORY.mdへ公開日時・版数・変更概要・採用ZIPを記録し、対象ソースと公開ファイルをコミットする。公開指示に基づきpushする。

## ブログからの最新バイナリー導線

- ブログ本文はGitHubのソース一式と `Distribution/README.md` を案内する。WordPressへ配布ZIPを新規アップロードしない。
- 新しい配布ZIPを公開するときは、このREADMEの製品別最新版・ZIPリンク・SHA256リンクも同じコミットで更新する。過去のZIPは同じ版数で上書きしない。
- Issues、Pull requests、Discussions、Wiki、Projects、Actionsは無効。外部の共同編集者を追加しない。
- 秘密情報スキャンとpush protection、Dependabot通知を有効化し、既定ブランチのforce push・削除を禁止する。
- コメント等のinteraction limitはGitHubの上限6か月で設定する。恒久設定ではないため、期限前に設定を再確認する。
